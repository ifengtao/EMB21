/**
 *  求两个数的最大数
 */
#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;
    if(a > b) {
        printf("a和b中的大值为a=%d\n", a);
    } else {
        printf("a和b中的大值为b=%d\n", b);
    }
    return 0;
}