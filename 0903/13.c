#include <stdio.h>
/**
 * - 逗号运算符
 *      - 优先级最低，比=还低
 *      - 可以逗号后面可以拿到前面的执行结果
 */
int main() {
    // 声明多个变量
    int x=1,y=2,z=3; // 逗号运算符优先级低于赋值运算符
    int a = (x+=20, y+=30, 40, 50,x+y); // (10,20,30,40,50)是逗号构建表达式，表达式会返回最后一个值
    printf("a=%d\n", a);
    return 0;
}